# 🚗 Car Classification Telegram Bot

Telegram-бот для распознавания марок автомобилей по фотографии. Проект основан на методах Computer Vision (CV) и использует State-of-the-Art архитектуру **Vision Transformer (ViT)**.

Бот умеет классифицировать 5 марок автомобилей: `Audi`, `Bentley`, `BMW`, `Porsche`, `Toyota`.

## 🧠 О модели и результатах обучения

В рамках проекта было проведено исследование и сравнение двух архитектур нейронных сетей с использованием Transfer Learning:

1.  **ResNet50** (Convolutional Neural Network)
2.  **Vision Transformer** (google/vit-base-patch16-224)

Обучение проводилось с использованием техник аугментации данных, стратифицированного разбиения выборки и тонкой настройки (fine-tuning).

### Сравнительная таблица результатов:

| Модель | Accuracy (Test) | Комментарий |
| :--- | :--- | :--- |
| **Vision Transformer (ViT)** | **~93.2%** | **Выбрана для продакшена** (лучшее обобщение и точность) |
| ResNet50 | ~85.0% | Использовалась для бенчмарка |

*Подробный код обучения и экспериментов находится в папке `notebooks/`.*

## 🛠 Технический стек

*   **ML/DL:** PyTorch, Transformers (Hugging Face), scikit-learn, Pillow.
*   **Backend:** Python 3, aiogram (асинхронный фреймворк для Telegram).
*   **Deployment/Tools:** Git LFS (для версионирования весов модели), Pydantic (для управления конфигурацией).

## 📂 Структура проекта

```text
car-classification-bot/
├─ notebooks/                            # Jupyter-ноутбуки с экспериментами
│  ├─ vision_transformer.ipynb           # Обучение финальной модели ViT (93% acc)
│  └─ resnet_experiment.ipynb            # Эксперименты с ResNet50 (85% acc)
├─ src/
│  ├─ bot/
│  │  ├─ __init__.py                     # Инициализация бота
│  │  └─ handlers.py                     # Обработчики сообщений и логика общения
│  ├─ services/
│  │  └─ predict.py                      # Сервис инференса (асинхронная обертка над моделью)
│  ├─ models/
│  │  └─ vit.py                          # Класс-обертка для Vision Transformer
│  ├─ utils/
│  │  └─ image_loader.py                 # Утилиты для обработки изображений
│  ├─ main.py                            # Точка входа (запуск бота)
│  └─ config.py                          # Конфигурация проекта (переменные окружения)
├─ saved_models/
│  └─ vit_model.pth                      # Веса модели (хранятся через Git LFS)
├─ requirements.txt                      # Зависимости проекта
├─ .env.example                          # Пример файла с переменными окружения
└─ README.md
```text

## 🚀 Установка и запуск
Для запуска проекта локально выполните следующие шаги:

1. Клонирование репозитория
Так как веса модели хранятся через Git LFS, убедитесь, что он у вас установлен.

bash
git lfs install
git clone https://github.com/ВАШ_НИК/car-vision-bot.git
cd car-classification-bot
2. Создание виртуального окружения
bash
# Windows
python -m venv venv
.\venv\Scripts\activate

# Linux/macOS
python3 -m venv venv
source venv/bin/activate
3. Установка зависимостей
bash
pip install -r requirements.txt
4. Настройка переменных окружения
Создайте файл .env в корне проекта и добавьте туда токен вашего Telegram-бота (получить у @BotFather) и путь к модели:

env
BOT_TOKEN=ваш_токен_от_BotFather
VIT_MODEL_PATH=saved_models/vit_model.pth
5. Запуск бота
bash
python src/main.py
После запуска отправьте боту фотографию любой машины из 5 поддерживаемых классов, и он вернет предсказание с уверенностью в процентах.
